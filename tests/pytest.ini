[pytest]
# Add the tests/ directory to sys.path so local modules (shared, httpcodes, etc.) are importable
pythonpath = .
# Only collect classes whose names start with "Test" — prevents APIBase from being collected
python_classes = Test*
# Only look for test files matching test_*.py in this directory
python_files = test_*.py
